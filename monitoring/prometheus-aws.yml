global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'coupon-rush'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['APP_PRIVATE_IP:8080']  # terraform apply 후 실제 IP로 교체
